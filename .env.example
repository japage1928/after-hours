# xAI (mash plans + Write tab vocals) — optional
XAI_API_KEY=

# Postgres — Neon Free pooled URI for deploy (leave unset for local PGLite)
DATABASE_URL=

# Also accepted if you use another host / marketplace sync:
# POSTGRES_URL=
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=
